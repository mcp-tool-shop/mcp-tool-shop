[
  {
    "repo": "brand",
    "toolName": "Brand",
    "tag": "v1.0.6",
    "name": "brand v1.0.6",
    "body": [
      "Feature pass: first-class galleries + dynamic README sync. Dogfood swarm, grounded in a 4-question study-swarm + retrieval-verified citation recovery.",
      "`brand add-gallery <slug> <source-dir>`** — explicit, idempotent registration of a directory of images as a named gallery collection for a slug. Full resync on re-run (adds/updates/removes to match `source-dir`, content-hash compared, never mtime), natural-sort default order, `--order` for explicit ordering via numeric-prefix renaming, `--dry-run`. Auto-regenerates `manifest.json`.",
      "`brand sync --slug <slug> [--check]`** — regenerates a consuming repo's README gallery block from the manifest, via a new `<!-- brand:gallery:start slug=\"...\" -->` / `<!-- brand:gallery:end -->` marker convention (namespaced so future block types don't collide). `--check` reports drift without writing (CI-gatable, exit 1 on drift). Pure function of the local manifest + local README — no network calls. Deterministic output: byte-identical across runs with unchanged inputs.",
      "Manifest asset entries now carry an explicit `role: \"primary\" | \"gallery\"`** (plus `gallery: \"<folder>\"` for gallery entries). `generateManifest()` moved from an unscoped recursive glob to a bounded two-level scan (`<slug>/readme.<ext>` = primary, `<slug>/<oneFolder>/<file>` = gallery) — zero migration needed, every existing slug's file coverage is unchanged.",
      "`brand audit` is role-aware.** The `multiple-logo-matches` check no longer false-flags a legitimate gallery as a badge collision; a README with unmanaged gallery `<img>` tags gets a new informational `unmanaged-gallery` finding pointing at `brand sync` instead.",
      "`docs/handbook.md` §10 \"Galleries & Dynamic READMEs\" — the design rationale and prior-art grounding (doctoc, terraform-docs, Kubernetes' verify-codegen.sh, Bazel's bare-glob warning, Storybook/Astro's readdir-order caution)."
    ],
    "publishedAt": "2026-07-01T06:29:25Z",
    "url": "https://github.com/mcp-tool-shop-org/brand/releases/tag/v1.0.6",
    "prerelease": false
  },
  {
    "repo": "sprite-foundry-packs",
    "toolName": "Sprite Foundry Packs",
    "tag": "pirate-raiders-3d-2-v1.0.0",
    "name": "@sprite-foundry/pirate-raiders-3d-2 1.0.0",
    "body": [
      "feat: pirate-raiders-3d-2 — next-gen true-3D pre-render pack (26 chars) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/sprite-foundry-packs/pull/12"
    ],
    "publishedAt": "2026-07-01T05:05:34Z",
    "url": "https://github.com/mcp-tool-shop-org/sprite-foundry-packs/releases/tag/pirate-raiders-3d-2-v1.0.0",
    "prerelease": false
  },
  {
    "repo": "sensor-humor",
    "toolName": "Sensor Humor",
    "tag": "v1.2.1",
    "name": "sensor-humor v1.2.1",
    "body": [
      "Dockerfile now copies `tsconfig.build.json` so the GHCR image builds. v1.2.0's `build` script",
      "became `tsc -p tsconfig.build.json` (to keep the dev-only scorecard out of the published dist),",
      "but the Docker build only copied `tsconfig.json`, so the v1.2.0 GHCR image failed. The npm",
      "package, GitHub Release, and Pages site for v1.2.0 were unaffected; this patch is npm-identical",
      "to 1.2.0 apart from the Docker build fix."
    ],
    "publishedAt": "2026-06-30T09:07:00Z",
    "url": "https://github.com/mcp-tool-shop-org/sensor-humor/releases/tag/v1.2.1",
    "prerelease": false
  },
  {
    "repo": "sensor-humor",
    "toolName": "Sensor Humor",
    "tag": "v1.2.0",
    "name": "sensor-humor v1.2.0",
    "body": [
      "Dogfood swarm: a research study-swarm grounded the design, then a full health pass",
      "(bug/security + proactive + humanization) and a feature pass. 187 → 294 tests.",
      "v1.2 Prompt Stability Lock** — a *form + safety* regression scorecard (explicitly NOT a",
      "\"funniness\" metric). A deterministic golden set + statistics run in `npm test`; `npm run",
      "scorecard` runs the live drift gate: per-mood conformance over N samples, gated on a **Wilson**",
      "score interval with a **three-valued PASS / FAIL / INCONCLUSIVE** verdict (FAIL only when the"
    ],
    "publishedAt": "2026-06-30T08:45:45Z",
    "url": "https://github.com/mcp-tool-shop-org/sensor-humor/releases/tag/v1.2.0",
    "prerelease": false
  },
  {
    "repo": "research-os",
    "toolName": "Research Os",
    "tag": "v0.14.0",
    "name": "v0.14.0",
    "body": [
      "A two-stage hardening pass over the v0.13.1 surface. **No new pipeline layers, gates, closure ledgers, or reviewer models** — every change is a defect fix or an additive guard/observability field within the locked architecture. Default (no-failure) behavior is byte-identical; the four frozen packs and the locked operator surfaces (error codes, the four help topics, CLI summary labels) are unchanged. 1663 → 1834 tests.",
      "Discover-time SSRF guard.** `research-os discover run` fetches LLM-proposed URLs to title-check them; that fetch now routes through the same private/loopback/link-local + DNS guard that `gather` uses (factored into `src/sources/url-safety.ts`), **including a post-redirect re-check** so a public URL that 30x-redirects to `127.0.0.1` / `169.254.169.254` / an internal host is refused before its body is read. The discover intake filter now also rejects `http:` (was silently allowed).",
      "`research-os query` no longer crashes on hyphenated terms.** FTS5 search of ordinary research vocabulary (`machine-learning`, `cost-benefit`, `peer-reviewed`) previously aborted with a raw SQLite error; terms are now quoted as phrases, and any residual FTS error surfaces as a structured `INDEX_QUERY_INVALID` with a quoting hint.",
      "`review --review-window 0` (or negative), and `--per-source-cap 0`/negative, no longer hang.** Both flags now require a positive integer; the reviewer and triage seams also clamp defensively.",
      "Freeze integrity at the immutability boundary.** `freeze` now refuses (rather than silently passing) when a live `claims.jsonl` / `claim-reviews.jsonl` line is malformed, when two same-timestamp review rows for one claim disagree (the canonical conflict guard `pack publish` already enforced — now shared via `getEffectiveDecisionMap`), and when a synthesis report contains a malformed `[claim:…]` citation. New refusal codes: `FREEZE_INCOMPATIBLE_REVIEW_DECISIONS`, `FREEZE_MALFORMED_CITATION`.",
      "`review promote` reads the shipped calibration receipts.** The canonical `seeded-v1.json` profile receipts are *aggregate* receipts; `loadReceiptForPack` now reads them (was hard-aborting `CALIBRATION_RECEIPT_MALFORMED` on a valid receipt). It also no longer mis-blocks promotion of a clean section that contains `frame_excluded` claims."
    ],
    "publishedAt": "2026-06-30T04:08:00Z",
    "url": "https://github.com/mcp-tool-shop-org/research-os/releases/tag/v0.14.0",
    "prerelease": false
  },
  {
    "repo": "sprite-foundry-packs",
    "toolName": "Sprite Foundry Packs",
    "tag": "pirate-raiders-3d-v1.0.1",
    "name": "@sprite-foundry/pirate-raiders-3d 1.0.1",
    "body": [
      "fix(pirate-raiders-3d): 2-row lineup preview (v1.0.1) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/sprite-foundry-packs/pull/11"
    ],
    "publishedAt": "2026-06-27T02:00:38Z",
    "url": "https://github.com/mcp-tool-shop-org/sprite-foundry-packs/releases/tag/pirate-raiders-3d-v1.0.1",
    "prerelease": false
  },
  {
    "repo": "sprite-foundry-packs",
    "toolName": "Sprite Foundry Packs",
    "tag": "pirate-raiders-3d-v1.0.0",
    "name": "@sprite-foundry/pirate-raiders-3d 1.0.0",
    "body": [
      "feat: pirate-raiders-3d — 26 fantasy raiders, true-3D pre-rendered 2.5D pack by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/sprite-foundry-packs/pull/10"
    ],
    "publishedAt": "2026-06-27T01:50:56Z",
    "url": "https://github.com/mcp-tool-shop-org/sprite-foundry-packs/releases/tag/pirate-raiders-3d-v1.0.0",
    "prerelease": false
  },
  {
    "repo": "sprite-foundry-packs",
    "toolName": "Sprite Foundry Packs",
    "tag": "fantasy-villains-hd-v1.0.1",
    "name": "@sprite-foundry/fantasy-villains-hd 1.0.1",
    "body": [
      "fix(fantasy-villains-hd): shoulder the reaver's cleaver (v1.0.1) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/sprite-foundry-packs/pull/9"
    ],
    "publishedAt": "2026-06-25T03:03:10Z",
    "url": "https://github.com/mcp-tool-shop-org/sprite-foundry-packs/releases/tag/fantasy-villains-hd-v1.0.1",
    "prerelease": false
  },
  {
    "repo": "sprite-foundry-packs",
    "toolName": "Sprite Foundry Packs",
    "tag": "pirate-raiders-hd-v1.0.0",
    "name": "@sprite-foundry/pirate-raiders-hd 1.0.0",
    "body": [
      "feat: pirate-raiders-hd — 26 fantasy multi-species raiders (HD 2.5D pack) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/sprite-foundry-packs/pull/8"
    ],
    "publishedAt": "2026-06-25T02:22:42Z",
    "url": "https://github.com/mcp-tool-shop-org/sprite-foundry-packs/releases/tag/pirate-raiders-hd-v1.0.0",
    "prerelease": false
  },
  {
    "repo": "registry-stats",
    "toolName": "Registry Stats",
    "tag": "v3.4.0",
    "name": "v3.4.0 — GitHub Releases provider",
    "body": [
      "GitHub Releases provider** — a sixth registry alongside npm, PyPI, NuGet, VS Code Marketplace, and Docker Hub.",
      "Identifier is a repository slug (`owner/repo`). Reports the cumulative download count of all uploaded release assets (binaries, checksums, SBOMs) summed across every release, plus release/asset counts and the latest tag.",
      "Query it: `registry-stats owner/repo -r github` or `stats('github', 'owner/repo')`.",
      "GitHub only counts manually-uploaded assets, not the auto-generated source archive, so source-only repos report `0`. The count is all-time cumulative (like NuGet/VS Code), so weekly deltas are derived from snapshot diffing.",
      "A `githubToken` option (wired to the CI `GITHUB_TOKEN`) raises the API rate limit from 60 to 5000 requests/hour.",
      "Published via npm Trusted Publishing (OIDC) with Sigstore provenance. Full notes in [CHANGELOG.md](https://github.com/mcp-tool-shop-org/registry-stats/blob/main/CHANGELOG.md)."
    ],
    "publishedAt": "2026-06-24T14:46:42Z",
    "url": "https://github.com/mcp-tool-shop-org/registry-stats/releases/tag/v3.4.0",
    "prerelease": false
  },
  {
    "repo": "backprop-trace",
    "toolName": "Backprop Trace",
    "tag": "v1.0.0",
    "name": "v1.0.0",
    "body": [
      "v1.0.0 — the sober promotion.** A comprehensive dogfood swarm took",
      "backprop-trace from honest mid-v0 to a v1.0.0 that meets the product's own gate",
      "criteria. Tests 792 → 940; CPU-only; 26 reconciler rules.",
      "SGD coupled-L2 weight decay** (the documented Rule 7 third branch) for plain",
      "SGD and SGD-momentum (classical / Nesterov / dampening). Coupled, not",
      "decoupled — the decay folds into the gradient and enters the momentum buffer,"
    ],
    "publishedAt": "2026-06-23T22:06:46Z",
    "url": "https://github.com/mcp-tool-shop-org/backprop-trace/releases/tag/v1.0.0",
    "prerelease": false
  },
  {
    "repo": "xrpl-lab",
    "toolName": "Xrpl Lab",
    "tag": "v2.2.0",
    "name": "v2.2.0 — Re-swarm: health pass + 6 features",
    "body": [
      "A full dogfood re-swarm over the shipped v2.0.0 — a four-stage health pass (every finding cross-family verified) plus six new capabilities.",
      "Payment Channels** (`payment_channel_101`) — XRP micropayments: lock once, sign many off-ledger claims for free, settle the latest on-ledger. The native rail for tipping / pay-per-action / streaming rewards.",
      "Token Freeze** (`token_freeze_101`) — Individual + Global Freeze, the issuer's non-destructive sanction levers below clawback.",
      "MPT distribution** (`mpt_distribution_101`) — the missing half: a holder opts in (`MPTokenAuthorize`), then the issuer actually pays the token to a player.",
      "Browser proof verifier** — a dashboard Verify page + `POST /api/verify`: offline SHA-256 + on-ledger `--live` re-fetch. The \"anyone can re-check this proof\" promise without the CLI.",
      "Artifact download** from the dashboard + `cohort-status --format csv` (gradebook roster)."
    ],
    "publishedAt": "2026-06-22T19:15:53Z",
    "url": "https://github.com/mcp-tool-shop-org/xrpl-lab/releases/tag/v2.2.0",
    "prerelease": false
  },
  {
    "repo": "repo-knowledge",
    "toolName": "Repo Knowledge",
    "tag": "v2.1.1",
    "name": "v2.1.1",
    "body": [
      "Patch release: a cross-platform fix for npm version sync plus two CLI gap-closers. No breaking changes.",
      "`rk versions --refresh` now syncs the npm channel on Windows.** The npm version worker shelled out to `npm view`, which is unspawnable via `execFileSync` on Windows — npm is a `.cmd` shim, so a bare name throws `ENOENT` (PATHEXT resolves only through a shell) and naming `npm.cmd` throws `EINVAL` under the Node CVE-2024-27980 mitigation. The npm channel therefore only ever refreshed on POSIX. It now reads the registry packument's `time` object over HTTP (`registry.npmjs.org/<name>`) with the shared transient-retry helper — no subprocess, no PATH/CLI dependency — matching the existing PyPI worker. `rk health`'s `npm audit` + `npx` toolchain probes (which genuinely need the CLI) now spawn through a shell on Windows only, while POSIX keeps its no-shell, injection-proof path.",
      "`rk classify <slug>`** — set the curated lifecycle fields `--status` (`active`/`paused`/`archived`/`unknown`), `--stage` (free-form, e.g. `shipped`), and `--category` (`product`/`tool`/`library`/`experiment`/`blueprint`/`marketing`). These columns are populated by neither `sync` nor `scan` (the GitHub metadata shape carries none of them), so without this command every repo sat at `status='unknown'`. Status and category are enum-validated; `\"\"` clears `stage`/`category`.",
      "`rk note --delete`** — retire a superseded note by its `--type` + `--title` key (the same key `note` dedupes on). Notes were previously append/replace-only with no removal path. `--content` is now optional (required only when adding)."
    ],
    "publishedAt": "2026-06-22T04:07:57Z",
    "url": "https://github.com/mcp-tool-shop-org/repo-knowledge/releases/tag/v2.1.1",
    "prerelease": false
  },
  {
    "repo": "websketch-ir",
    "toolName": "Websketch Ir",
    "tag": "v2.3.0",
    "name": "v2.3.0",
    "body": [],
    "publishedAt": "2026-06-21T19:30:19Z",
    "url": "https://github.com/mcp-tool-shop-org/websketch-ir/releases/tag/v2.3.0",
    "prerelease": false
  },
  {
    "repo": "audiobooker",
    "toolName": "Audiobooker",
    "tag": "v2.1.1",
    "name": "audiobooker v2.1.1",
    "body": [
      "<!-- Release notes generated using configuration in .github/release.yml at v2.1.1 -->",
      "fix(ci): correct gh-action-pypi-publish SHA (PyPI publish failed on v2.1.0) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/audiobooker/pull/12",
      "fix(v2.1.1): absolute README logo + nav URLs (render on PyPI) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/audiobooker/pull/13"
    ],
    "publishedAt": "2026-06-21T11:32:30Z",
    "url": "https://github.com/mcp-tool-shop-org/audiobooker/releases/tag/v2.1.1",
    "prerelease": false
  },
  {
    "repo": "audiobooker",
    "toolName": "Audiobooker",
    "tag": "v2.1.0",
    "name": "audiobooker v2.1.0",
    "body": [
      "<!-- Release notes generated using configuration in .github/release.yml at v2.1.0 -->",
      "chore: bump GH Actions to Node 24-compatible releases (Sep 2026 deadline) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/audiobooker/pull/7",
      "ci: bump Actions to Node 24 (June 16 deprecation) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/audiobooker/pull/9",
      "Dogfood swarm → v2.1.0: health pass + ~36 features + npm launcher by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/audiobooker/pull/10",
      "ci: rename publish.yml → release.yml (match npm TP) + ship npm translations by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/audiobooker/pull/11",
      "@mcp-tool-shop made their first contribution in https://github.com/mcp-tool-shop-org/audiobooker/pull/7"
    ],
    "publishedAt": "2026-06-21T11:16:10Z",
    "url": "https://github.com/mcp-tool-shop-org/audiobooker/releases/tag/v2.1.0",
    "prerelease": false
  },
  {
    "repo": "attestia",
    "toolName": "Attestia",
    "tag": "v2.0.2",
    "name": "@mcptoolshop/attestia v2.0.2",
    "body": [
      "release: @mcptoolshop/attestia v2.0.2 (npm README logo + translations) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/attestia/pull/17"
    ],
    "publishedAt": "2026-06-21T10:20:31Z",
    "url": "https://github.com/mcp-tool-shop-org/attestia/releases/tag/v2.0.2",
    "prerelease": false
  },
  {
    "repo": "attestia",
    "toolName": "Attestia",
    "tag": "v2.0.1",
    "name": "@mcptoolshop/attestia v2.0.1",
    "body": [
      "feat(npm): single bundled @mcptoolshop/attestia + OIDC release by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/attestia/pull/13",
      "chore(ci): Node 22 + 24 (drop stale Node 20) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/attestia/pull/14",
      "release: @mcptoolshop/attestia v2.0.1 (first npm publish) by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/attestia/pull/15",
      "fix(ci): build before test in release.yml by @mcp-tool-shop in https://github.com/mcp-tool-shop-org/attestia/pull/16"
    ],
    "publishedAt": "2026-06-21T10:03:49Z",
    "url": "https://github.com/mcp-tool-shop-org/attestia/releases/tag/v2.0.1",
    "prerelease": false
  },
  {
    "repo": "repomesh",
    "toolName": "Repomesh",
    "tag": "v2.3.0",
    "name": "v2.3.0 — verifier-plugin contract (#7): extend the network by data, not code",
    "body": [
      "The **verifier-plugin contract** (#7). The trust network is now extended by editing data, not code: the check-kinds registry, scoring weights, attestor-gated set, scoreable-results, and node-kind→event-type permissions live in `verifier.policy.json` (v2), read through a resolver with per-field v1 fallback. Additive — existing scoring + validation are byte-identical to v2.2.0.",
      "Data-driven check-kinds registry** — adding a check kind (e.g. `sast.scan`) or verifier node is a ~6-line `verifier.policy.json` edit + a `node.json` in a PR; no code change. Guide: [docs/verifier-plugin-contract.md](https://github.com/mcp-tool-shop-org/repomesh/blob/main/docs/verifier-plugin-contract.md).",
      "registered ≠ trusted** made explicit — an unregistered check kind earns zero + is flagged; a registered kind still needs a trusted-set consensus pass.",
      "`schemas/verifier.policy.schema.json`** — the policy is schema-validated (v1 + v2); the ledger validator **fails closed** on a malformed trust policy.",
      "`verifier.policy.json` → `v: 2`; every v2 field optional with per-field fallback to the pre-#7 default. A v1 policy is unchanged. `trust.json` regenerates byte-identical to v2.2.0 — the refactor is provably behavior-preserving. Tests 912 → 939. Cross-family verified (DeepSeek-671B): no fail-open.",
      "Full detail: [CHANGELOG.md](https://github.com/mcp-tool-shop-org/repomesh/blob/main/CHANGELOG.md)."
    ],
    "publishedAt": "2026-06-21T08:13:49Z",
    "url": "https://github.com/mcp-tool-shop-org/repomesh/releases/tag/v2.3.0",
    "prerelease": false
  },
  {
    "repo": "attestia",
    "toolName": "Attestia",
    "tag": "v2.0.0",
    "name": "v2.0.0 — Financial truth, hardened + durable",
    "body": [
      "Second-cycle dogfood swarm on the v1.0.1 base: deep health pass → feature pass → composed re-audit → treatment. Every finding was audit-generated by parallel agents and **independently verified by a cross-family review** (different model families) before any fix; each fix shipped with a regression test proven to fail against the old code first.",
      "Tests: 2,228 → 2,564, all green.**",
      "Merkle tree now uses RFC 6962 odd-node promotion** instead of self-duplication (CVE-2012-2459 class): a tree over `[a,b,c]` and one over `[a,b,c,c]` previously produced the same root, allowing a forged inclusion proof for a phantom leaf against a genuine published root. **Merkle root values change** — any attestation proof minted under v1.x must be regenerated. This is the only breaking change.",
      "Security (3 CRITICAL + 14 HIGH + composed-seam fixes):** Merkle forgery; production fail-open auth → fail closed; cross-currency fund-hiding in reconciliation; event-store tamper-evidence + concurrency hardening; multi-sig idempotency + quorum-downgrade binding; Sybil-resistant consensus; XRPL partial-payment accounting; money-on-float + atomic distribution/payroll; funding separation-of-duties bound to the authenticated caller.",
      "Durability (new):** the running service now survives restart — opt-in durable hash-chained event log (audit truth) + snapshot restore, per-tenant.",
      "API/SDK/OpenAPI (new):** 26 authenticated endpoints (treasury, vault, governance) + full OpenAPI 3.1 (drift-guarded) + SDK namespaces + `paginateAll`."
    ],
    "publishedAt": "2026-06-21T05:51:44Z",
    "url": "https://github.com/mcp-tool-shop-org/attestia/releases/tag/v2.0.0",
    "prerelease": false
  },
  {
    "repo": "repomesh",
    "toolName": "Repomesh",
    "tag": "v2.2.0",
    "name": "v2.2.0 — dogfood swarm: ledger tamper-evidence + adopter/operator features",
    "body": [
      "A full dogfood-swarm release: health pass (bug/security → proactive → humanization → a11y) + feature pass. Additive — existing ledger and node data verify unchanged. Tests 709 → 912. Shipcheck 100%. Cross-family verified (DeepSeek-671B + GPT-OSS-120B).",
      "Ledger tamper-evidence covered only the first **8 of 47** events (validate-ledger skipped v2 manifests; only the genesis partition was committed; the daily anchor cron's PRs piled up unmerged, freezing main). Now verifies v1 **and** v2 manifests, commits a self-maintaining whole-ledger `all.json`, recomputes roots in `verify-release`, and auto-merges anchors on green — a reorder/drop of any event (e.g. a `KeyRevocation`) is caught locally.",
      "Programmatic **library API** (`import { verifyRelease, … } from \"@mcptoolshop/repomesh\"`)",
      "`repomesh keygen`** + `init --second-key` + a TUF §6.1 ≥2-key separation-of-duties advisory",
      "Machine-readable **`status.json`** network-health endpoint (frozen-ledger signal)",
      "CI failure alerting (deduped tracking issue) on both crons"
    ],
    "publishedAt": "2026-06-21T05:11:36Z",
    "url": "https://github.com/mcp-tool-shop-org/repomesh/releases/tag/v2.2.0",
    "prerelease": false
  },
  {
    "repo": "backpropagate",
    "toolName": "Backpropagate",
    "tag": "v1.7.0",
    "name": "Backpropagate v1.7.0",
    "body": [
      "Card-aware full fine-tuning ceiling.** `mode=\"full\"` no longer caps at a hard 4B. The ceiling is derived from the 4-addend training-memory arithmetic against detected VRAM — **16 GB → 4B, 24 GB → 5B, 32 GB → 6B** pure-GPU (`_full_ft_ceiling_for_vram`). Override explicitly with `--full-ft-ceiling-billions` / `Trainer(full_ft_ceiling_billions=...)`.",
      "FSDP2 CPU-offload full fine-tuning — single-card 7B-class.** `--full-ft-offload` / `Trainer(full_ft_offload=True)` configures FSDP2 `fully_shard` + `CPUOffloadPolicy` + activation checkpointing + bf16 to spill params + optimizer state into host RAM, lifting the full-FT ceiling to **~8B / 7B-class on a 32 GB card** (+~64 GB host RAM). It's a documented escape hatch: slower (PCIe/CPU-bandwidth-bound) and needs an NCCL backend (Linux/WSL2). On a host without NCCL (e.g. Windows-native) it fails fast with the new `DEP_FSDP_UNAVAILABLE` rather than silently running without offload and OOMing. The trainer initializes a single-process group automatically (no `accelerate launch` / `torchrun` needed).",
      "24–34B QLoRA model presets.** New presets for the 32 GB envelope: `qwen2.5-14b` (~8.5 GB, the daily-driver sweet spot), `mistral-small-24b` (~18 GB), `qwen2.5-32b` (~26 GB, just fits at `max_len 2048`), and `llama-3.1-8b`. rank=alpha per size, paged 8-bit AdamW.",
      "32 / 48 GB batch + VRAM tiers.** `Trainer._detect_batch_size` and the `estimate-vram` tier table gained 32 GB (batch 6) and 48 GB (batch 8) tiers. `estimate_vram(..., offload=True)` models the FSDP2 offload path and reports a new `host_ram_gb`.",
      "Contrastive full-FT error.** `RUNTIME_FULL_FT_MODEL_TOO_LARGE` now names the right recovery for the situation — `--full-ft-offload` when the model fits the offload ceiling, LoRA/QLoRA when it's past even that.",
      "Repositioned for the 32 GB envelope.** README, handbook (full-fine-tuning, cli-reference, error-codes, estimate-vram), and presets now lead with single-card 7B-class full fine-tuning + 24–34B QLoRA → Ollama, with an honest \"scales down to 16 GB\" note. The old \"7B+ full FT → use HuggingFace/A100\" anti-pitch is replaced by the `--full-ft-offload` path."
    ],
    "publishedAt": "2026-06-21T01:11:57Z",
    "url": "https://github.com/mcp-tool-shop-org/backpropagate/releases/tag/v1.7.0",
    "prerelease": false
  },
  {
    "repo": "backpropagate",
    "toolName": "Backpropagate",
    "tag": "v1.6.0",
    "name": "Backpropagate v1.6.0",
    "body": [
      "SimPO — reference-free preference tuning.** `--method simpo` / `Trainer(method=\"simpo\")` trains on `{prompt, chosen, rejected}` preference data via TRL's `CPOTrainer` with `loss_type=\"simpo\"` (`cpo_alpha=0`) — length-normalized reward + target margin, no reference model, same single-GPU LoRA envelope as ORPO/SFT (SimPO: Meng et al. 2024, [arXiv:2405.14734](https://arxiv.org/abs/2405.14734)). Defaults `--simpo-beta 2.0` / `--simpo-gamma 1.0`; the LR auto-lowers to `1e-6` (clamped if set ≥ `1e-5` — high LR degrades SimPO). `mode=\"lora\"` only.",
      "KTO — preference tuning on unpaired binary feedback.** `--method kto` / `Trainer(method=\"kto\")` trains on an **unpaired** `{prompt, completion, label}` dataset (per-example thumbs-up/down) via TRL's `KTOTrainer` (KTO: Ethayarajh et al. 2024, [arXiv:2402.01306](https://arxiv.org/abs/2402.01306)) — the data shape ORPO/SimPO/DPO can't consume. New `DatasetFormat.KTO` + `DatasetLoader.to_kto_dataset()` + detection/validation. KTO uses the frozen LoRA base as its reference (no second model loaded — 16 GB envelope preserved); `--kto-beta 0.1`; desirable/undesirable weights auto-balance from the dataset's label counts toward a 1:1–4:3 effective ratio. `mode=\"lora\"` only. Both new methods ship with non-mocked GPU smokes (real CPOTrainer/KTOTrainer trains, finite loss, loadable + mergeable adapter).",
      "Eval task-metrics + conjunction merge-gate.** `backprop eval --metric <name> --references <jsonl> [--gate-metric <name>]` adds deterministic, judge-free task metrics — `normalized_exact_match`, `token_f1` (SQuAD-style), `contains`, `regex`, `pass_rate` — to `EvalResult` (`task_metrics`, `eval_n`, bootstrap `metric_ci`). No LLM judge. The eval-gate is now a **conjunction**: a merge/checkpoint is accepted only if held-out loss does **not** regress AND every gated task-metric does not regress beyond a noise band (delta vs the bootstrap CI, not zero); it warns when the held-out set is underpowered (`eval_n < 100`). Closes the gap where a merge could improve loss while regressing actual task behavior and still pass. (Grounded in HELM [arXiv:2211.09110], SQuAD EM/F1 [arXiv:1606.05250], and the AlpacaEval null-model result [arXiv:2410.07137].)",
      "`backprop generate <adapter_path> \"<prompt>\"`** — ad-hoc \"did my fine-tune work?\" generation against a trained adapter path (no recorded run, no GGUF export). Base model inferred from `adapter_config.json`; `--base`, `--max-new-tokens`, `-n/--num`, `--temperature`.",
      "`backprop data split <jsonl>`** — deterministic train / held-out split (`--heldout-ratio`, `--seed`, `--out-train`, `--out-heldout`), completing the held-out/contamination workflow that `data report --against` and `eval --heldout` assume.",
      "FP8 compute path is now verified.** The v1.5 experimental FP8 path (`--fp8`, torchao float8 on Blackwell/Hopper sm_90+) is dogfood-verified end-to-end on an RTX 5090 (Blackwell sm_120) — trains, the `Float8Linear` base / bf16-adapter exclusion holds, and the result merges. Status promoted experimental → verified."
    ],
    "publishedAt": "2026-06-20T21:16:22Z",
    "url": "https://github.com/mcp-tool-shop-org/backpropagate/releases/tag/v1.6.0",
    "prerelease": false
  },
  {
    "repo": "brand",
    "toolName": "Brand",
    "tag": "v1.0.5",
    "name": "brand v1.0.5",
    "body": [
      "CI hardening + npm trusted-publishing migration.",
      "CI**: `lint-test-build`'s `npm audit --audit-level=high` step had been failing on a high-severity `vite` advisory (GHSA-fx2h-pf6j-xcff `server.fs.deny` bypass + GHSA-v6wh-96g9-6wx3 launch-editor NTLMv2) and a moderate `brace-expansion` DoS — both transitive dev/build deps. `npm audit fix` bumped 8 lockfile packages (no direct-dep change); re-audit reports 0 vulnerabilities. Unblocks the required check so logo-asset PRs merge through the gate again.",
      "Publishing migrated to npm Trusted Publishing (OIDC).** New `release.yml` publishes `@mcptoolshop/brand` on a `v*` tag push via OIDC (`--provenance`, no `NPM_TOKEN`), bound to the npm Trusted Publisher. The token-based `publish.yml` is retired."
    ],
    "publishedAt": "2026-06-20T21:07:15Z",
    "url": "https://github.com/mcp-tool-shop-org/brand/releases/tag/v1.0.5",
    "prerelease": false
  },
  {
    "repo": "tool-compass",
    "toolName": "Tool Compass",
    "tag": "v2.4.0",
    "name": "Tool Compass v2.4.0",
    "body": [
      "Dogfood swarm v3 — full health pass (bug/security → proactive → humanization",
      "→ visual) + a user-scoped feature pass. Tests 991 → 1151; ruff errors 31 → 0.",
      "No breaking changes; the default config path, the Ollama embedding default,",
      "and all existing tool/CLI behavior are unchanged.",
      "`tool-compass init`** — scaffolds `compass_config.json` at the resolved",
      "config path and prints a ready-to-paste Claude Desktop `mcpServers` snippet."
    ],
    "publishedAt": "2026-06-20T19:38:02Z",
    "url": "https://github.com/mcp-tool-shop-org/tool-compass/releases/tag/v2.4.0",
    "prerelease": false
  },
  {
    "repo": "repo-knowledge",
    "toolName": "Repo Knowledge",
    "tag": "v2.1.0",
    "name": "v2.1.0 — dogfood swarm: health pass + feature wave",
    "body": [
      "Dogfood-swarm release: a full four-stage health pass (bug/security → proactive → humanization → visual) plus a feature wave. No breaking changes — every v2.0.0 callsite continues to work. Tests **377 → 515**.",
      "MCP surface 19 → 30 tools.** Health: `health_feed`, `health_doctor`, `health_portfolio`. Operational hygiene: `db_fsck` (discloses its `db_health_runs` write), `repo_diff`, `ops_runs`. Lifecycle: `archive_repo` (canonical-slug echo + optional reason note), `delete_repo` (explicit `confirm: true` gate — the only irreversible MCP op). Publish: `repo_versions`. Dogfood/audit: `suggest_dogfood` (repo/surface mutual-exclusion), `audit_failing` (domain enum). All are reads over already-exported builders; none triggers a network refresh.",
      "`--json` on the core query commands** — `list`, `find`, `show`, `related`, `stats`, and the five `audit` reads now emit parseable JSON (a not-found slug emits `{error:\"not_found\"}` + exit 1, so a `jq` pipeline never gets empty input).",
      "`rk backup` / `rk restore`** — first-class DB snapshots (`VACUUM INTO`); restore validates `schema_version`, is confirm-gated, swaps atomically (temp-then-rename), and clears WAL sidecars so a stale WAL can't replay over the restored file.",
      "Auto-snapshot before migrations** — `openDb()` snapshots a pre-existing DB to `data/backups/` before the migration ladder runs (skipped for fresh DBs and when `RK_NO_MIGRATION_BACKUP` is set).",
      "`rk doctor`** — environment preflight (config, DB, schema-version, `gh auth`, rig, recent runs) with `--json` / `--strict`. **`rk config` / `rk config validate`** — print/validate the resolved config (flags the `your-github-org` placeholder)."
    ],
    "publishedAt": "2026-06-20T11:53:25Z",
    "url": "https://github.com/mcp-tool-shop-org/repo-knowledge/releases/tag/v2.1.0",
    "prerelease": false
  },
  {
    "repo": "roll",
    "toolName": "Roll",
    "tag": "v2.1.0",
    "name": "roll v2.1.0",
    "body": [
      "A full health-pass + feature-pass release. Hardening across the engine and the",
      "two wire surfaces, an honesty pass on the probability claims, and a new family",
      "of analysis capabilities for game-balance work. Test suite grew 226 → 509.",
      "Table analysis** — `analyzeTable` / `analyzeCollection` compute a loot/encounter",
      "table's *exact* outcome distribution: each entry's real selection probability",
      "(after weights, level filtering, and conditions, matching what `weightedSelect`"
    ],
    "publishedAt": "2026-06-20T09:31:18Z",
    "url": "https://github.com/mcp-tool-shop-org/roll/releases/tag/v2.1.0",
    "prerelease": false
  },
  {
    "repo": "sensor-humor",
    "toolName": "Sensor Humor",
    "tag": "v1.1.1",
    "name": "sensor-humor v1.1.1",
    "body": [
      "npm README rendering: the logo and CI badge did not render on npmjs.com (npm does not proxy the absolute brand-repo image URL or the GitHub Actions `badge.svg`). The logo is now a repo-local `logo.png` referenced relatively, and the CI badge uses shields.io — both render on npm and GitHub."
    ],
    "publishedAt": "2026-06-20T08:50:27Z",
    "url": "https://github.com/mcp-tool-shop-org/sensor-humor/releases/tag/v1.1.1",
    "prerelease": false
  },
  {
    "repo": "sensor-humor",
    "toolName": "Sensor Humor",
    "tag": "v1.1.0",
    "name": "sensor-humor v1.1.0",
    "body": [
      "Comprehensive dogfood swarm: health pass (bug/security + proactive + humanization), a feature wave, and the full treatment.",
      "Persistent session** — opt-in disk persistence (`SENSOR_HUMOR_PERSIST`) to `~/.sensor-humor/session.json` (override dir via `SENSOR_HUMOR_SESSION_DIR`). Callbacks survive a full server restart; the file auto-expires after 24h",
      "Ollama Cloud auth** — `OLLAMA_API_KEY` sent as a `Bearer` header to a remote/cloud `OLLAMA_HOST`; never logged, persisted, or echoed",
      "Degradation signal** — comedy tools return `degraded` + `degraded_reason` when output is a fallback (backend down) or a safety substitution, so a caller can tell a real joke from a canned one",
      "`debug_status` now reports live Ollama reachability, whether the model is pulled, resolved host/timeout/prompt-version, whether an API key is set, and generation stats (total/fallback calls, last reason, last latency)",
      "Startup health check verifies the configured model is **pulled**, not just that the daemon answers — prints the exact `ollama pull <model>` fix when it is missing"
    ],
    "publishedAt": "2026-06-20T08:30:42Z",
    "url": "https://github.com/mcp-tool-shop-org/sensor-humor/releases/tag/v1.1.0",
    "prerelease": false
  },
  {
    "repo": "world-forge",
    "toolName": "World Forge",
    "tag": "v4.5.0",
    "name": "v4.5.0 — town structures + world modeling",
    "body": [
      "Dogfood-swarm feature wave. Every addition is backward-compatible — projects authored on v4.x validate and open unchanged.",
      "Tile pipeline** — image-backed tilesets + colored-rect fallback, paint brush, per-tile \"Solid\" walkability, Godot `TileMapLayer` export.",
      "Interiors** — prop placement + tile wall collision.",
      "Town economy** — market nodes + crafting stations (store, editor panel, Godot export).",
      "Town structures** — `Building` / `Hub` / `Stronghold` as three placed types (panel + Godot export).",
      "World modeling** (research-grounded):"
    ],
    "publishedAt": "2026-06-19T21:43:10Z",
    "url": "https://github.com/mcp-tool-shop-org/world-forge/releases/tag/v4.5.0",
    "prerelease": false
  },
  {
    "repo": "synthesis",
    "toolName": "Synthesis",
    "tag": "v1.2.0",
    "name": "v1.2.0 — grounded_uptake (the positive witness) + relational_posture",
    "body": [
      "The positive-capability release. `performative_empathy` refuses to certify sincerity (no deterministic feature can). v1.2.0 adds the checker that certifies the **narrower, observable, auditable** claim instead: *grounded uptake was performed*.",
      "Three states (`verified_uptake` / `no_verified_uptake` / `not_applicable`); `pass` is always true (a positive witness never fails a case). `verified_uptake` requires ALL FIVE witnesses: a grounded anchor in a **declarative clause**, **non-parroting** (recombined, outside any verbatim 3-gram), a **support move** (question/offer/interpretation), **template containment**, and **safety compatibility** (composes `agency_language` + `unverifiable_reassurance` plus a conservative directive/guarantee screen). It measures observable behavior, not inner state — so it never certifies sincerity, quality, or full safety (Jacobs & Wallach 2021). Earned through a 54-candidate adversarial red-team.",
      "Rolls the checkers into one verdict (`unsafe_comfort` / `hollow_warmth_flagged` / `pivot_or_abandonment` / `grounded_uptake_verified` / `unresolved_abstain`) and carries explicit **`non_claims`** so a positive verdict can never be over-read.",
      "Complementarity:** a genuine grounded reply makes `performative_empathy` abstain (it never certifies sincerity) while `grounded_uptake` verifies — same reply, both honest.",
      "Fully deterministic (zero-LLM). 177 tests; eval 0 unexpected, label accuracy 63/63 (100%). Scope and red-team-earned limits documented in `docs/KNOWN-LIMITATIONS.md`; verified citations in `docs/study-grounding.md`."
    ],
    "publishedAt": "2026-06-16T21:21:26Z",
    "url": "https://github.com/mcp-tool-shop-org/synthesis/releases/tag/v1.2.0",
    "prerelease": false
  },
  {
    "repo": "synthesis",
    "toolName": "Synthesis",
    "tag": "v1.1.0",
    "name": "v1.1.0 — the empathy-theater detector",
    "body": [
      "A fourth deterministic checker that detects **empathy-theater**: generic warmth-template padding deployed over a vulnerable disclosure that engages nothing of the user's specific content.",
      "It is a pure **detector** — it flags unmistakable theater or abstains, and it **never certifies a response as sincere**. Five adversarial rounds plus a concreteness measurement established that no deterministic, zero-LLM feature can separate genuine engagement from gamed content-free padding, so the tool refuses to make the positive claim at all. It is **precision-favoring**: it would rather miss some theater than false-flag a genuine reply, and a register-neutral engagement gate protects brief/non-native/dialect replies.",
      "Honest label accuracy — N/A checks no longer counted as passes",
      "Grounded regex patterns — fewer false positives/negatives across all checkers (MISC/SDT/RIAS/Stiles-grounded)",
      "Functional npm packaging, `--fail-on` validation, deterministic report ordering",
      "Zero-LLM · fully deterministic · 148 tests · MIT."
    ],
    "publishedAt": "2026-06-16T19:25:13Z",
    "url": "https://github.com/mcp-tool-shop-org/synthesis/releases/tag/v1.1.0",
    "prerelease": false
  },
  {
    "repo": "xrpl-lab",
    "toolName": "Xrpl Lab",
    "tag": "v2.0.0",
    "name": "XRPL Lab v2.0.0",
    "body": [
      "Prove It For Real** — a major release that makes the \"prove by artifact\" thesis literally true, completes every transaction lifecycle the curriculum teaches, and adds a game-economy controls track. Driven by a full dogfood swarm (health pass A/B/C/D + a three-wave feature pass + final test), every change adversarially verified.",
      "Ledger-anchored proof verification.** `xrpl-lab proof verify <file> --live` and `cert-verify <file> --live` now re-fetch every claimed transaction from the public XRPL and confirm it is `validated`, succeeded (`tesSUCCESS`), and matches the pack's recorded account/type — so a proof is checkable against the ledger by anyone, with no trust in the learner's machine and no server. A pack with a fabricated txid still passes the offline SHA-256 hash but now **fails** the live check. Dry-run packs honestly report \"no on-ledger transactions to verify\"; mixed packs verify only their real-network legs. The original (no-`--live`) hash check is unchanged.",
      "Complete transaction lifecycles** — no module teaches half a story anymore:",
      "Escrow finish & cancel** (`EscrowFinish` / `EscrowCancel`) + a new `escrow_finish_101` module: create a time-locked escrow, release it after `FinishAfter`, or reclaim it after `CancelAfter`. (Previously XRP could be locked in an escrow with no way to release it.)",
      "DID delete** (`DIDDelete`) — `did_101` now closes the identity lifecycle (anchor → verify → revoke).",
      "NFT burn** (`NFTokenBurn`) — `nft_minting_101` now teaches freeing the reserve by burning the asset."
    ],
    "publishedAt": "2026-06-16T15:06:03Z",
    "url": "https://github.com/mcp-tool-shop-org/xrpl-lab/releases/tag/v2.0.0",
    "prerelease": false
  },
  {
    "repo": "loadout-os",
    "toolName": "Loadout Os",
    "tag": "v1.0.2",
    "name": "loadout-os v1.0.2",
    "body": [],
    "publishedAt": "2026-06-16T14:56:14Z",
    "url": "https://github.com/mcp-tool-shop-org/loadout-os/releases/tag/v1.0.2",
    "prerelease": false
  },
  {
    "repo": "loadout-os",
    "toolName": "Loadout Os",
    "tag": "v1.0.1",
    "name": "loadout-os v1.0.1",
    "body": [],
    "publishedAt": "2026-06-16T14:15:28Z",
    "url": "https://github.com/mcp-tool-shop-org/loadout-os/releases/tag/v1.0.1",
    "prerelease": false
  },
  {
    "repo": "loadout-os",
    "toolName": "Loadout Os",
    "tag": "v1.0.0",
    "name": "loadout-os v1.0.0",
    "body": [],
    "publishedAt": "2026-06-16T13:38:37Z",
    "url": "https://github.com/mcp-tool-shop-org/loadout-os/releases/tag/v1.0.0",
    "prerelease": false
  },
  {
    "repo": "site-theme",
    "toolName": "Site Theme",
    "tag": "v2.1.0",
    "name": "v2.1.0 — front-door MCP + executable doctest",
    "body": [
      "front-door MCP server** — `front-door mcp` starts a zero-dependency stdio JSON-RPC server exposing `front_door_verify` (the structured scorecard) and `front_door_standard`; exported at `@mcptoolshop/site-theme/front-door/mcp`.",
      "Executable doctest channel** — `front-door verify --run-doctests` compiles/runs fenced JS examples rustdoc-style; `+SKIP`/`ignore` → UNBACKED. Opt-in and safe (default `verify` stays read-only).",
      "Executable ablation runner** — `front-door ablation` implements the three-arm docs-on/off protocol from `EVAL.md`.",
      "Minor bump: new agent surface + opt-in execution channels; the verify gate and programmatic API are backward-compatible."
    ],
    "publishedAt": "2026-06-16T10:12:23Z",
    "url": "https://github.com/mcp-tool-shop-org/site-theme/releases/tag/v2.1.0",
    "prerelease": false
  },
  {
    "repo": "shipcheck",
    "toolName": "Shipcheck",
    "tag": "v1.0.6",
    "name": "v1.0.6 — front-door gate + OIDC publishing",
    "body": [
      "Gate G — AI-native front door**: `shipcheck front-door [--root <dir>] [--json]` runs the front-door verifier (`verify()` from `@mcptoolshop/site-theme/front-door`, an optional peer dependency), folds its risk-ordered scorecard into shipcheck, exits non-zero when the front-door gate fails, and skips gracefully when the dependency is absent.",
      "Publishing migrated to **OIDC trusted publishing** (`release.yml`); the legacy token-based `publish.yml` was removed.",
      "All 7 translated READMEs refreshed."
    ],
    "publishedAt": "2026-06-16T10:05:29Z",
    "url": "https://github.com/mcp-tool-shop-org/shipcheck/releases/tag/v1.0.6",
    "prerelease": false
  },
  {
    "repo": "site-theme",
    "toolName": "Site Theme",
    "tag": "v2.0.0",
    "name": "v2.0.0 — front-of-house",
    "body": [
      "site-theme grows a second pillar: **front-door**, the AI-native front-door verifier.",
      "front-door** — `verify` routes documented claims in README / AGENTS.md / llms.txt to evidence channels (references, minimality, doctest, attestation, gherkin), reports a risk-ordered four-bucket scorecard, and exits non-zero when the gate fails.",
      "CLI: `site-theme front-door verify | init | standard | eval`. Programmatic `verify()` at `@mcptoolshop/site-theme/front-door` (consumed by shipcheck).",
      "6th **`tool`** template + `ToolSiteConfig` for CLI / MCP / npm package pages.",
      "129 tests · biome clean · self-eval 7/7 · site-theme passes its own front-door verify (0 findings).",
      "Major bump signals the new front-of-house pillar; the theme + template API is backward-compatible."
    ],
    "publishedAt": "2026-06-16T08:20:00Z",
    "url": "https://github.com/mcp-tool-shop-org/site-theme/releases/tag/v2.0.0",
    "prerelease": false
  },
  {
    "repo": "escape-the-valley",
    "toolName": "Escape The Valley",
    "tag": "v1.1.1",
    "name": "Escape the Valley v1.1.1",
    "body": [
      "Voice extra now installs.** The `voice` extra pinned `voice-soundboard>=2.5`,",
      "which was never published to PyPI — so `pip install \"escape-the-valley[voice]\"`",
      "(and the binary build, which resolved the extra) failed. `voice-soundboard`",
      "2.5.2 is now on PyPI; the extra pins `voice-soundboard[kokoro]>=2.5` so the",
      "configured Kokoro voices synthesize out of the box.",
      "Binary build no longer resolves optional extras.** `release-binaries.yml`"
    ],
    "publishedAt": "2026-06-15T23:58:04Z",
    "url": "https://github.com/mcp-tool-shop-org/escape-the-valley/releases/tag/v1.1.1",
    "prerelease": false
  },
  {
    "repo": "escape-the-valley",
    "toolName": "Escape The Valley",
    "tag": "v1.1.0",
    "name": "Escape the Valley v1.1.0",
    "body": [
      "Streaming GM narration:** the storyteller now writes token-by-token, visibly composing each beat as Ollama generates it, threaded through the async TUI so the narration appears live instead of arriving in one block after a pause.",
      "Graded endings + epilogue:** a run now ends with a graded `EndingResult` — **triumphant**, **weathered**, **pyrrhic**, or **lost** — read from survivors, days-versus-par, taboo, and uncanny, and narrated as a proper epilogue rather than a bare cause-of-death line.",
      "Real stakes:** data-driven events can now wound or kill the party. A new event health path plus roughly 30 curated bodily-danger events mean a bad choice can cost a life, with attributed death causes (an event-wound death reads as *Injury*, not a generic \"The trail\").",
      "Run artifacts:** an end-of-run screen surfaces the XRPL postcard, the run's stats, and an export/share path, so a finished run leaves something you can keep and pass along.",
      "Cross-family GM panel simulator** (`scripts/sim_gm_panel.py`): validates GM behavior across model families, so the narrator can be checked for tone and safety on more than one backend.",
      "Multiplayer parcel trading** over XRPL: `trail parcel send <addr> <supply> <amount>`, `trail parcel list`, `trail parcel accept <id>`, `trail parcel sent`, and `trail wallet share` — send supply tokens to another traveler via a Testnet micropayment with an attached memo, and accept incoming parcels into your backpack."
    ],
    "publishedAt": "2026-06-15T23:01:21Z",
    "url": "https://github.com/mcp-tool-shop-org/escape-the-valley/releases/tag/v1.1.0",
    "prerelease": false
  },
  {
    "repo": "xrpl-lab",
    "toolName": "Xrpl Lab",
    "tag": "v1.8.0",
    "name": "XRPL Lab v1.8.0",
    "body": [
      "KB-sourced module expansion — four new tracks and four new modules, built from the verified",
      "xrpl-knowledge corpus, plus a transport fix that restores the live-network path.",
      "Four new tracks** (nfts, tokens, payments, identity) with four new modules, each teaching one",
      "capability and verifying it on-ledger:",
      "`nft_minting_101` (nfts) — mint a native NFT game asset (NFTokenMint, XLS-20): taxon, URI, royalty.",
      "`mpt_issuance_101` (tokens) — issue a Multi-Purpose Token game currency (MPTokenIssuanceCreate, XLS-33)."
    ],
    "publishedAt": "2026-06-15T02:51:17Z",
    "url": "https://github.com/mcp-tool-shop-org/xrpl-lab/releases/tag/v1.8.0",
    "prerelease": false
  },
  {
    "repo": "repomesh",
    "toolName": "Repomesh",
    "tag": "v2.1.0",
    "name": "v2.1.0 — time-aware key rotation + revocation",
    "body": [
      "Time-aware **key rotation and revocation**. Closes a live trust bug: key resolution was untimed, so a compromised-but-still-listed key verified `VALID`.",
      "Windowed maintainer keys (`validFrom`/`validUntil`/`revokedAt`/`revocationReason`/`invalidAfter`, RFC 5280) + `KeyRotation`/`KeyRevocation` events + `repomesh key rotate|revoke`.",
      "Time-aware verification at all 11 resolution sites against the XRPL anchor close-time; routine rotation is prospective, compromise is retroactive.",
      "Stripped-`node.json` resistance: windows are re-derived order-aware from the signed + anchored events and merged most-restrictively, so a tampered manifest cannot revive a revoked key.",
      "Non-destructive — window-less keys grandfather. 709 tests (was 414); verified across 3 cross-family adversarial passes. New [threat model](https://github.com/mcp-tool-shop-org/repomesh/blob/main/docs/threat-model.md).",
      "🤖 Generated with [Claude Code](https://claude.com/claude-code)"
    ],
    "publishedAt": "2026-06-14T23:57:00Z",
    "url": "https://github.com/mcp-tool-shop-org/repomesh/releases/tag/v2.1.0",
    "prerelease": false
  },
  {
    "repo": "prism-verify",
    "toolName": "Prism Verify",
    "tag": "v1.6.0",
    "name": "prism-verify 1.6.0 — family-AB harder corpus + round-robin CLI",
    "body": [
      "@-"
    ],
    "publishedAt": "2026-06-14T22:45:30Z",
    "url": "https://github.com/mcp-tool-shop-org/prism-verify/releases/tag/v1.6.0",
    "prerelease": false
  },
  {
    "repo": "repomesh",
    "toolName": "Repomesh",
    "tag": "v2.0.0",
    "name": "RepoMesh v2.0.0",
    "body": [
      "A full trust-correctness hardening pass plus a verifier-fleet humanization and adoption layer. The trust model is materially stronger and now **fail-closed**, and the verification surface is legible and CI-friendly. Tests 58 → 414 (0 fail). Implemented via version-dispatch, so existing committed ledger and anchor data keeps verifying unchanged.",
      "Verification is fail-closed.** `verify-release` returns `UNVERIFIED` (never `PASS`) for a self-signed, wrong-repo-signed, non-allowlisted-attestor, or failed-attestation release; strict `--anchored` fails when the on-chain anchor cannot be verified. Tri-state exit codes — **PASS=0, FAIL=1, UNVERIFIED=3** — with `--fail-on <fail|unverified>`.",
      "Attestor authorization enforced** (trusted-attestor allowlist, bundled as a non-removable floor in the CLI).",
      "Stricter assurance scoring** — a verifier that could not run scores `unscored` = 0 credit; missing/unverified SBOM digest earns no credit; repo overrides can't raise a failing check's weight.",
      "Real XRPL anchor verification (account allowlist + `validated` + `tesSUCCESS` + on-chain memo binding; Merkle root recomputed from the manifest's pinned window).",
      "GitHub Action** release-gate wrapper · **`verify-all`** batch · **SARIF + Markdown** output · **`--local`** offline verification · **/repos** Trust Index browse page · per-version **badge proof-chain** pages · **OSV result cache**."
    ],
    "publishedAt": "2026-06-14T19:20:19Z",
    "url": "https://github.com/mcp-tool-shop-org/repomesh/releases/tag/v2.0.0",
    "prerelease": false
  },
  {
    "repo": "xrpl-lab",
    "toolName": "Xrpl Lab",
    "tag": "v1.7.1",
    "name": "XRPL Lab v1.7.1",
    "body": [
      "Packaging + CI follow-up to v1.7.0 — completes the npm distribution channel, patches three dependency CVEs, and fixes the dependency-audit gate.",
      "Dependency security floors.** Patched three known CVEs by pinning to fixed releases and re-locking. Two are **runtime** floors shipped to users: `idna>=3.15` (CVE-2026-45409, transitive via httpx) and `starlette>=1.0.1` (PYSEC-2026-161, transitive via fastapi). One is a **dev** floor: `urllib3>=2.7.0` (PYSEC-2026-141/142, transitive via pip-audit/requests). The audited tree now carries no known CVEs.",
      "npm distribution now publishes via OIDC trusted publishing.** `@mcptoolshop/xrpl-lab` is published from this repo's new `release.yml` (on tag push) using npm provenance + OIDC — **no tokens**. The binary-launcher wrapper (`package.json` + `bin/xrpl-lab.js`) now lives in-repo beside the Python package; `npx @mcptoolshop/xrpl-lab` downloads the SHA-256-verified release binary for the matching tag.",
      "`pip-audit` gate fixed.** Dropped `--strict`: it makes pip-audit exit non-zero whenever a dependency is *skipped*, including the editable self-package we skip with `--skip-editable`, so the audit failed even with zero vulnerabilities (`distribution marked as editable`). Without `--strict` the audit still fails on any *real* vulnerability. The single residual advisory against `pip` itself — the installer toolchain, not a declared dependency — is explicitly ignored; `verify.sh` updated to match."
    ],
    "publishedAt": "2026-06-14T17:23:31Z",
    "url": "https://github.com/mcp-tool-shop-org/xrpl-lab/releases/tag/v1.7.1",
    "prerelease": false
  },
  {
    "repo": "xrpl-lab",
    "toolName": "Xrpl Lab",
    "tag": "v1.7.0",
    "name": "v1.7.0 — Testnet Enforcement, Network Honesty + Dashboard Redesign",
    "body": [
      "A re-audit dogfood swarm (Stage A security → B/C proactive + behavioral health → D Claude Design redesign), each wave audit → amend → adversarially verified. **702 → 785 tests; shipcheck 100%.**",
      "Testnet-only is now enforced in code**, not just a default + docs. `classify_network()` (fail-closed, suffix-spoof resistant) gates all five signing methods **and** the faucet — a mainnet/unknown `XRPL_LAB_RPC_URL`/`XRPL_LAB_FAUCET_URL` override is refused before the wallet seed touches the network. Labels (`network_name`, `/api/status`, `doctor`) report the real endpoint.",
      "PyPI publish action SHA-pinned; no path leakage across the WebSocket boundary (session-export manifest + runner save guards).",
      "Artifacts no longer mislabel dry-run/devnet runs as `testnet` — `state.network` is stamped per-run, and per-tx explorer links resolve from each tx's own network (no more dead `testnet.xrpl.org` links in a sealed dry-run proof pack).",
      "`GET /api/status` returns `network` + `version`; new `GET /api/health` instant liveness probe.",
      "A full **\"calm instrument panel\"** redesign (via Claude Design): sidebar shell, a real type system (embedded offline), status as shape + glyph + label + hue, an accessible ARIA tablist, and a **branded focus-trapped modal replacing native `confirm()`/`alert()`**."
    ],
    "publishedAt": "2026-06-14T16:44:45Z",
    "url": "https://github.com/mcp-tool-shop-org/xrpl-lab/releases/tag/v1.7.0",
    "prerelease": false
  },
  {
    "repo": "registry-stats",
    "toolName": "Registry Stats",
    "tag": "v3.3.1",
    "name": "v3.3.1 — dev-dependency security upgrade",
    "body": [
      "Dev-dependency security upgrade (esbuild/tsup/vitest/Astro). **No consumer-facing change** — the published package keeps **zero runtime dependencies** and the same build output (ESM + CJS + `.d.ts`).",
      "Eliminated the high/critical **esbuild** advisories ([GHSA-gv7w-rqvm-qjhr](https://github.com/advisories/GHSA-gv7w-rqvm-qjhr), [GHSA-g7r4-m6w7-qqqr](https://github.com/advisories/GHSA-g7r4-m6w7-qqqr) — both patched in esbuild `0.28.1`) from **both** npm trees by pinning `esbuild` via `overrides`, and restored the full-tree `npm audit` gate (removing the temporary `--omit=dev` scope from v3.3.0).",
      "Engine:** `tsup` 8.0→8.5, `vitest` 3→4 (271 tests pass, no test changes).",
      "Dashboard:** `astro` 5→6 + `@astrojs/starlight` 0.37→0.40 + `vite` pinned ^7 — clears the three Astro-own CVEs (XSS in `define:vars`, allowlist bypass, server-island replay).",
      "Published via OIDC Trusted Publishing (`release.yml`) with Sigstore provenance. Full notes: [CHANGELOG](https://github.com/mcp-tool-shop-org/registry-stats/blob/main/CHANGELOG.md)."
    ],
    "publishedAt": "2026-06-14T15:24:49Z",
    "url": "https://github.com/mcp-tool-shop-org/registry-stats/releases/tag/v3.3.1",
    "prerelease": false
  },
  {
    "repo": "prism-verify",
    "toolName": "Prism Verify",
    "tag": "v1.5.0",
    "name": "prism-verify 1.5.0 — valid family-different A/B",
    "body": [
      "The validity-clean rebuild of prism's flagship Lock-1 measurement (\"family-different verification helps\").",
      "Family-different A/B v2 — the within-judge round-robin.** A family-provenanced, execution-labeled corpus (each model generates the code; hidden tests label it — no LLM grader), and a within-judge estimator where the verifier's *capability cancels inside the contrast*, so `self_preference(V) = false_accept_rate(V on own-family bugs) − …(other-family bugs)` is family self-preference, not a size artifact. mid-p McNemar + cluster (problem) bootstrap + BH-FDR; tri-state collapse; discrimination-floor gate. First valid pilot published in `eval/RESULTS.md` (an interpretable ceiling-effect null — the v1.4.0 result was a confound).",
      "Configurable verifier registry (F-14).** `PRISM_VERIFIER_MODEL_<FAMILY>` + `PRISM_<PROVIDER>_BASE_URL` make a cross-family seat (e.g. Ollama-Cloud `gpt-oss:120b-cloud`) first-class env config. A source scan confines the measurement-only `allow_same_family` bypass to its legitimate sites.",
      "Dated-Anthropic-model default → durable alias `claude-haiku-4-5`.",
      "Stale runtime version: `prism.__version__` was pinned at 1.0.0 since v1.0 (surfaced by `prism --version` + the HTTP version).",
      "Tests 641 → 692; ruff + mypy-strict clean; security/stats-sensitive waves cross-family-reviewed."
    ],
    "publishedAt": "2026-06-14T14:28:25Z",
    "url": "https://github.com/mcp-tool-shop-org/prism-verify/releases/tag/v1.5.0",
    "prerelease": false
  },
  {
    "repo": "registry-stats",
    "toolName": "Registry Stats",
    "tag": "v3.3.0",
    "name": "v3.3.0",
    "body": [
      "Dogfood swarm release. Engine tests 236→271; desktop tests revived (0-run → 13 in CI); zero runtime dependencies unchanged.",
      "Real portfolio **diversityTrend** (half-window Gini) replacing a hardcoded placeholder.",
      "`stats.all()`/`stats.compare()` surface per-registry failures (additive `errors` channel + CLI stderr warnings) instead of silently dropping a down registry.",
      "`serve --host` / `--cors` flags; `registerProvider()` validation.",
      "Dashboard: WCAG-AA light-theme contrast, reduced-motion, ARIA for the co-pilot/charts, stale-snapshot warning.",
      "Centralized package-name validation across all engine entry points (closed a request-path-manipulation gap on the npm bulk path; docker rejects `..`)."
    ],
    "publishedAt": "2026-06-14T14:22:03Z",
    "url": "https://github.com/mcp-tool-shop-org/registry-stats/releases/tag/v3.3.0",
    "prerelease": false
  }
]
